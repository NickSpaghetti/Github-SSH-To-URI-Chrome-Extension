# Copy to .env and fill in. Get CLIENT_ID/CLIENT_SECRET from a "Desktop app"
# OAuth client in Google Cloud Console with the Chrome Web Store API enabled.
CHROME_CLIENT_ID=
CHROME_CLIENT_SECRET=

# Populated automatically by `make refresh-chrome-token`
CHROME_REFRESH_TOKEN=
